apiVersion: v1
kind: Service
metadata:
    name: "kwetter-efa5"
    labels:
        app: "kwetter-efa5"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "kwetter-efa5"